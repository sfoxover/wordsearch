<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="false"
             x:Class="WordSearch.Controls.TileControl">
  <ContentView.Content>
        <Frame x:Name="TileFrame" BorderColor="{Binding TitleBorderColor}" Padding="1" CornerRadius="5" WidthRequest="{Binding TileWidth}" HeightRequest="{Binding TileHeight}">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding TileClickCommand}"/>
            </Frame.GestureRecognizers>
            <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                <Label Text="{Binding Letter}" FontSize="Large" TextColor="{Binding LetterTextColor}" BackgroundColor="{Binding LetterTextBkgColor}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"/>
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>