<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WordSearch.Views.MainPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Title="Super Word Search"
             xmlns:viewModels="clr-namespace:WordSearch.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:DesignMainPageViewModel}">
    <ContentPage.Resources>
        <StyleSheet Source="/Assets/Styles.css" />
    </ContentPage.Resources>
    <StackLayout Orientation="Vertical">
        <Image Source="{Binding LogoPath}" />
        <Button StyleClass="BlueButton" Text="Start new game" Command="{Binding NewEasyGameCommand}" HorizontalOptions="Center"></Button>
        <Slider Maximum="3" Minimum="1" HorizontalOptions="CenterAndExpand" VerticalOptions="Center" WidthRequest="500" />
    </StackLayout>

</ContentPage>
